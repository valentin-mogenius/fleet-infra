apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sftpgo
  namespace: default
spec:
  chart:
    spec:
      chart: sftpgo
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      version: "9.14.5"
  interval: 1m
  releaseName: sftpgo
